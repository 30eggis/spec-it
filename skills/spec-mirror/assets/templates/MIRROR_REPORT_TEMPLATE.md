# Spec Mirror Report

**분석 일시:** {{date}}
**원본 Spec:** {{original_spec_path}}
**코드베이스:** {{codebase_path}}

---

## 최종 판정

{{#if verdict_pass}}
### PASS

모든 Spec이 구현되었습니다. (over-spec {{over_count}}건 허용)
{{else}}
### FAIL

누락된 요구사항 {{missing_count}}건이 있습니다.
{{/if}}

---

## 요약

| 분류 | 개수 | 비율 | 판정 |
|------|------|------|------|
| ✓ 일치 | {{match_count}} | {{match_percent}}% | PASS |
| ✗ 누락 | {{missing_count}} | {{missing_percent}}% | FAIL |
| ➕ 오버스펙 | {{over_count}} | {{over_percent}}% | **허용** |
| ? 검토 필요 | {{review_count}} | {{review_percent}}% | - |

**총 요구사항:** {{total_count}}개

> **Over-spec 정책:** Spec에 없지만 구현된 추가 기능은 허용됩니다.
> Missing 항목만이 실패 조건이며, over-spec은 정보 제공 목적으로만 표시됩니다.

---

## 상세 분석

### ✓ 일치 항목

정상적으로 구현된 요구사항:

| 원본 REQ | 설명 | 구현 확인 |
|----------|------|-----------|
{{#each matched}}
| {{req_id}} | {{description}} | ✓ |
{{/each}}

### ✗ 누락 항목 (FAIL 조건)

Spec에 있지만 구현되지 않은 요구사항:

| 원본 REQ | 설명 | 우선순위 | 조치 |
|----------|------|----------|------|
{{#each missing}}
| {{req_id}} | {{description}} | {{priority}} | 구현 필요 |
{{/each}}

### ➕ 오버스펙 항목 (허용)

Spec에 없지만 구현된 기능 (참고용):

| 발견 기능 | 설명 | 상태 |
|-----------|------|------|
{{#each over}}
| {{feature}} | {{description}} | ✓ 허용됨 |
{{/each}}

### ? 검토 필요

매칭 확신도가 낮아 수동 검토 필요:

| 원본 REQ | 유사 구현 | 검토 사유 |
|----------|-----------|-----------|
{{#each review}}
| {{req_id}} | {{similar}} | {{reason}} |
{{/each}}

---

## 후속 조치 체크리스트

### 누락 항목 (필수)

{{#each missing}}
- [ ] **{{req_id}}**: {{description}} 구현 필요
{{/each}}

### 검토 필요 항목

{{#each review}}
- [ ] {{req_id}}: {{reason}} 확인
{{/each}}

---

## spec-it-execute 연동

```json
{
  "matchCount": {{match_count}},
  "missingCount": {{missing_count}},
  "overCount": {{over_count}},
  "verdict": "{{#if verdict_pass}}PASS{{else}}FAIL{{/if}}"
}
```

**다음 단계:**
{{#if verdict_pass}}
- Phase 6 (UNIT-TEST) 진행
{{else}}
- 누락 항목 재개발 → Phase 4 QA → Phase 5 재검증
{{/if}}

---

## 권장 사항

{{recommendations}}

---

*Generated by spec-mirror skill*
